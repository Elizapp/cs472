<%- include('partials/header') %>
<body>
    <header><h1>Welcome to Shopping</h1></header>
    <section>
        <h2>Products</h2>
        <div class="content-car">
            <% for (const item of products) { %>
                <div class="item">
                    <h3> <%= item.name %></h3>
                    <p>Price: US$ <%= item.price %></p>
                    <p>Description: <%= item.description %></p>
                    <form class="addProduct">
                        <input class="id" name="id" type="hidden" value="<%= item.id %>">
                        <input class="name" name="name" type="hidden" value="<%= item.name %>">
                        <input class="price" name="price" type="hidden" value="<%= item.price %>">
                        <input class="description" name="description" type="hidden" value="<%= item.description %>">
                        <button name="submit">Add to Cart</button>
                    </form>
                </div>
            <% } %>
        </div>
    </section>
    <%- include('partials/aside') %>
    <%- include('partials/footer') %>
